# Required metadata
sonar.projectKey=Jwright:DemoApp
sonar.projectVersion=trunk
sonar.projectName=AppCrud1

# Comma-separated paths to directories with sources (required)
sonar.sources=.

# Language
sonar.language=cs

# Encoding of the source files
sonar.sourceEncoding=UTF-8